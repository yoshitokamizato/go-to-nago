<%= content_for(:title, "メールアドレス変更") %>

<div class="login-area">
  <div class="list-title underline-blue">
    <h1>メールアドレスを変更</h1>
  </div>
  <p class="text-center mt-3">新しいメールアドレスを入力してください。</p>
  <p class="text-center mt-3">入力いただいたメールアドレスに確認メールを送信します。</p>
  <div class="mt-5">
    <%= form_for(resource ,as: resource_name, url:users_update_email_path(resource_name), html: { method: :put, class: "common-form" }) do |f| %>
    <%= bootstrap_devise_error_messages!%>
    <div class="form-group mb-3">
      <%= f.label :email %>
      <%= f.email_field :email, class:'form-control', autofocus: true, value:nil, placeholder: "メールアドレスを入力してください" %>
    </div>
    <%= f.submit "変更する", class:'button button-blue-color' %>
    <%= link_to "マイページヘ戻る", user_path ,class:"button button-gray-color" %>
    <% end %>
  </div>
</div>