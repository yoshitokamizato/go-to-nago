      <% @menus.each do |menu| %>
      <div class="col-6 col-sm-6 col-lg-3 show-row-list">
        <div class="card ">
          <%= image_tag(menu.image.thumb.url, class:"card-img-top" ,alt:"...") %>
          <div class="card-body">
            <h5 class="card-title"><%= menu.name %></h5>
            <p class="card-text"><%= menu.content %></p>
            <p class="card-text">¥ <%= menu.price %></p>
          </div>
        </div>
      </div>
      <% end %>
