
<h1>お問い合わせ(入力画面)</h1>
<p>問い合わせはこちらからお願いします。</p>

<% if @inquiry.errors.any? %>
  <div>
    <ul>
      <% @inquiry.errors.full_messages.each do |msg| %>
        <li><span style="color: red;"><%= msg %></span></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @inquiry, url: {action: "confirm"} do |f| %>

  <% if user_signed_in? %>
      <%= f.hidden_field :user_id, value: current_user.id %>
  
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control', value: current_user.nickname %>
    </div>

    <div class="form-group">
      <%= f.label :email %>
      <%= f.text_field :email, class: 'form-control', value: current_user.email %>
    </div>

  <% else %>
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control', value: @inquiry.name %>
    </div>
    <div class="form-group">
      <%= f.label :email %>
      <%= f.text_field :email, class: 'form-control' %>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :subject %>
    <%= f.text_field :subject, class: 'form-control', value: @inquiry.subject %>
  </div>

 <div class="form-group">
   <%= f.label :message %>
   <%= f.text_area :message, size: '10x10', class: 'form-control' %>
 </div>

  <%= f.submit '内容を確認する', class: 'btn btn-lg btn-primary btn-block' %>

<% end %>
